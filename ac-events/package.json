{
  "name": "ac-event-list",
  "version": "1.0.0",
  "description": "A list of events to trigger when certain things happen inside the ActiveCampaign CRM",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jordanskole",
  "license": "ISC",
  "dependencies": {
    "amplitude": "^3.5.0"
  }
}
